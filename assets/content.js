/* ============================================================
   Conteúdo da documentação Cryo · Burnout · Pyro.
   Cada `body` é markdown (renderizado por assets/app.js).
   Para evitar conflito com template literals, cada corpo é um
   array de linhas unido por "\n" (a função `md`).
   ============================================================ */
(function () {
  "use strict";
  function md() { return Array.prototype.join.call(arguments, "\n"); }

  var pages = [
    // =========================================================
    //  PRIMEIROS PASSOS
    // =========================================================
    {
      slug: "introducao", group: "Primeiros passos", title: "Introdução",
      lead: "Cryo é uma linguagem de alto nível que compila para **Pyro**, um bytecode próprio executado por uma VM. **Burnout** é o compilador que liga as duas pontas.",
      body: md(
        "O projeto é um sistema de linguagem em **três etapas**, com duas linguagens próprias e um compilador no meio:",
        "",
        "<div class=\"pipeline\">",
        "  <div class=\"stage\"><span class=\"st-tag\">1 · escreve</span><h4>Cryo <code>.cryo</code></h4><p>A linguagem que você escreve — tipada, de alto nível.</p></div>",
        "  <div class=\"stage\"><span class=\"st-tag\">2 · compila</span><h4>Burnout</h4><p>lexer → parser → AST → gerador de código.</p></div>",
        "  <div class=\"stage\"><span class=\"st-tag\">3 · executa</span><h4>Pyro <code>.pyro</code></h4><p>Bytecode próprio rodando na VM Pyro (em Go).</p></div>",
        "</div>",
        "",
        "O `.pyro` é compacto e opaco (a seção de código é codificada), roda na máquina através da VM e serve como **dado de treino nativo para agentes de IA** — a IA lê instruções já na forma que a máquina executa.",
        "",
        "## Por onde começar",
        "",
        "<div class=\"card-grid\">",
        "  <a class=\"card\" href=\"#/instalacao\"><span class=\"card-emoji\">⚙️</span><h4>Instalação</h4><p>O que você precisa: Python e (opcional) Go, gcc/MinGW.</p></a>",
        "  <a class=\"card\" href=\"#/inicio-rapido\"><span class=\"card-emoji\">🚀</span><h4>Início rápido</h4><p>Compile e rode seu primeiro programa em um minuto.</p></a>",
        "  <a class=\"card\" href=\"#/sintaxe\"><span class=\"card-emoji\">📖</span><h4>A linguagem</h4><p>Tipos, funções, controle de fluxo e operadores.</p></a>",
        "  <a class=\"card\" href=\"#/agent\"><span class=\"card-emoji\">🤖</span><h4>Agentes de IA</h4><p>schema, tools e o laço de agent nativo.</p></a>",
        "</div>",
        "",
        "## Uma amostra",
        "",
        "```cryo",
        "schema Fatura { string cliente; number total; string[] itens; }",
        "tool fn buscar_preco(string sku) -> number ={ return 19.90; }",
        "",
        "// structured output tipado e validado",
        "Fatura f = llm(\"gpt-x\", \"Extraia a fatura: \" + texto) as Fatura;",
        "",
        "// laço de tool-calling: o modelo pede tools, o runtime as executa",
        "string r = agent(\"gpt-x\", \"Preco do SKU-1 com 10% off?\");",
        "```",
        "",
        "## As três linguagens",
        "",
        "| Componente | Papel | Arquivos |",
        "|---|---|---|",
        "| **Cryo** | Linguagem-fonte (front-end → AST) | `cryo/lexer.py`, `parser.py`, `ast_nodes.py` |",
        "| **Burnout** | O compilador (CLI + geradores de código) | `burnout/cryoc.py`, `codegen_*.py` |",
        "| **Pyro** | Linguagem-alvo (bytecode + VM) | `pyro/vm/main.go`, `PYRO_BYTECODE.md` |",
        "",
        "> **Versão da linguagem:** v0.9. O backend **go** é o mais completo (structs, arrays, maps, JSON, opcionais, LLM); o backend **pyro** cobre o núcleo e está em expansão."
      )
    },
    {
      slug: "arquitetura", group: "Primeiros passos", title: "Arquitetura",
      lead: "Como um arquivo `.cryo` vira execução — do texto-fonte ao bytecode rodando na VM.",
      body: md(
        "## O caminho de compilação",
        "",
        "```text",
        "  escreve            compila (Burnout)              executa (VM Pyro)",
        " .cryo   ─────►   lexer → parser → AST → codegen   ─────►   .pyro + VM   ─────►  saída",
        "                            │",
        "             alvos alternativos: Go (.go) · C (.c) · x86-64 (.s)",
        "```",
        "",
        "1. **Lexer** (`cryo/lexer.py`) — transforma o texto em uma lista de tokens.",
        "2. **Parser** (`cryo/parser.py`) — constrói a AST (árvore sintática) a partir dos tokens.",
        "3. **Auditoria** (`cryo/security.py`, opcional) — percorre a AST em busca de riscos.",
        "4. **Codegen** (`burnout/codegen_*.py`) — gera o alvo escolhido.",
        "5. **Toolchain** — a VM Pyro (Go) executa o `.pyro`; ou `go`/`gcc` monta o binário nos outros alvos.",
        "",
        "## Componentes no disco",
        "",
        "```text",
        "Pyro_Cryo/",
        "├── cryo/       # CRYO    — a linguagem-fonte: lexer, parser, AST, análise + examples/",
        "├── burnout/    # Burnout — o compilador: cryoc.py, compiler.py, codegens, runtime, tests",
        "├── pyro/       # PYRO    — a linguagem-alvo: VM Pyro (Go) + especificação do bytecode",
        "└── build/      # artefatos gerados (.pyro, .go, .c, .s, binários) — git-ignored",
        "```",
        "",
        "A dependência é direta: **Burnout** consome o front-end de **CRYO** e, no alvo pyro, produz código para a **VM PYRO**.",
        "",
        "## Cinco alvos, um front-end",
        "",
        "O mesmo AST alimenta cinco geradores de código. Você escolhe com `--backend`:",
        "",
        "| Backend | Saída | Para quê |",
        "|---|---|---|",
        "| `go` (padrão) | `.go` → binário Go | Linguagem completa: LLM, maps, JSON, concorrência |",
        "| `pyro` | `.pyro` (bytecode) → VM | A linguagem-alvo própria; compacta e portável |",
        "| `node` | `.js` → executa com Node | Núcleo do Cryo em JavaScript (CommonJS) |",
        "| `c` | `.c` → binário via gcc | Núcleo + structs/arrays em C nativo |",
        "| `asm` | `.s` → binário via gcc | x86-64 (System V e Win64) |",
        "",
        "Veja [Backends](#/backends) para a matriz completa de cobertura."
      )
    },
    {
      slug: "instalacao", group: "Primeiros passos", title: "Instalação",
      lead: "O compilador Cryo é escrito em Python. As dependências extras dependem só do backend que você for usar.",
      body: md(
        "## Requisitos",
        "",
        "| Você quer... | Precisa de |",
        "|---|---|",
        "| Compilar `.cryo` (qualquer alvo) | **Python 3.10+** |",
        "| Rodar o alvo **go** (padrão) | **Go 1.18+** |",
        "| Rodar o alvo **pyro** (bytecode + VM) | **Go 1.18+** (compila a VM uma vez) |",
        "| Rodar o alvo **node** (JavaScript) | **Node.js** |",
        "| Rodar os alvos **c** / **asm** | **gcc** no `PATH` (MinGW no Windows) |",
        "",
        "> O compilador Python **sempre** gera o arquivo-fonte (`.go`, `.pyro`, `.c` ou `.s`). O toolchain só é necessário para montar/rodar o binário.",
        "",
        "## Obtendo o projeto",
        "",
        "```bash",
        "git clone https://github.com/Victor-477/Cryo.git      # front-end da linguagem",
        "git clone https://github.com/Victor-477/Burnout.git   # o compilador",
        "git clone https://github.com/Victor-477/pyro.git      # a VM / bytecode",
        "```",
        "",
        "O ponto de entrada é `burnout/cryoc.py`. Rode-o a partir da raiz do projeto.",
        "",
        "## Verificando",
        "",
        "```bash",
        "# roda a suíte de testes de fumaça (valida os geradores de código)",
        "python burnout/tests/test_smoke.py",
        "```",
        "",
        "Se você tem Go instalado, compile e rode um exemplo do núcleo:",
        "",
        "```bash",
        "python burnout/cryoc.py cryo/examples/example_bytecode.cryo --backend pyro --run",
        "```",
        "",
        "## Windows: build script pronto (backends c/asm)",
        "",
        "Para montar direto no Windows com MinGW, use o script — ele localiza o gcc (inclusive em instalações comuns do MSYS2/TDM/WinLibs), gera o assembly Win64 e monta o `.exe`:",
        "",
        "```bash",
        "REM cmd / Explorer",
        "burnout\\scripts\\build_win64.bat cryo\\examples\\example_asm.cryo --run",
        "```",
        "",
        "Se o gcc não estiver no `PATH`, o script imprime instruções de instalação e sai."
      )
    },
    {
      slug: "inicio-rapido", group: "Primeiros passos", title: "Início rápido",
      lead: "Escreva, compile e rode um programa Cryo em poucos comandos.",
      body: md(
        "## 1. Escreva um `.cryo`",
        "",
        "Crie `ola.cryo`:",
        "",
        "```cryo",
        "fn saudacao(string nome) -> string ={",
        "    return \"Ola, \" + nome + \"!\";",
        "}",
        "",
        "int[] numeros = [1, 2, 3, 4, 5];",
        "int soma = 0;",
        "for (int i = 0; i < len(numeros); i++) {",
        "    soma += numeros[i];",
        "}",
        "",
        "print(saudacao(\"Cryo\"));",
        "print(\"soma:\");",
        "print(soma);",
        "```",
        "",
        "> **Repare no `={`** — o corpo de uma função começa com o token `={` (e não `{`). É a sintaxe de corpo do Cryo.",
        "",
        "## 2. Compile e rode",
        "",
        "```bash",
        "# alvo go (padrão) — precisa apenas de Go instalado",
        "python burnout/cryoc.py ola.cryo --run",
        "",
        "# ou o alvo Pyro (bytecode próprio + VM)",
        "python burnout/cryoc.py ola.cryo --backend pyro --run",
        "```",
        "",
        "O `--run` compila e executa. Sem ele, apenas o artefato é gerado em `build/`.",
        "",
        "## 3. Explore",
        "",
        "```bash",
        "python burnout/cryoc.py ola.cryo --tokens     # imprime os tokens",
        "python burnout/cryoc.py ola.cryo --ast        # imprime a AST",
        "python burnout/cryoc.py ola.cryo --audit      # auditoria de segurança",
        "python burnout/cryoc.py ola.cryo --backend pyro --dis   # desassembla o bytecode",
        "```",
        "",
        "Pronto — a partir daqui, mergulhe na [sintaxe da linguagem](#/sintaxe) ou vá direto para [agentes de IA](#/agent)."
      )
    },

    // =========================================================
    //  A LINGUAGEM CRYO
    // =========================================================
    {
      slug: "sintaxe", group: "A linguagem Cryo", title: "Sintaxe básica",
      lead: "Variáveis, comentários, `print` e as regras gerais da linguagem.",
      body: md(
        "## Declaração de variáveis",
        "",
        "Cryo é **tipada estaticamente**. Toda variável declara seu tipo antes do nome:",
        "",
        "```cryo",
        "int    idade   = 30;",
        "number preco   = 19.90;",
        "string nome    = \"Ana\";",
        "bool   ativo   = true;",
        "```",
        "",
        "O ponto e vírgula encerra uma instrução. Espaços e quebras de linha são livres.",
        "",
        "## Constantes",
        "",
        "Use `const` para valores imutáveis (tipicamente no topo do arquivo):",
        "",
        "```cryo",
        "const number PI     = 3.14159265",
        "const string VERSAO = \"0.9.0\"",
        "const int    MASK   = 0xFF",
        "```",
        "",
        "## Comentários",
        "",
        "```cryo",
        "// comentário de linha",
        "",
        "/* comentário",
        "   de bloco */",
        "```",
        "",
        "## Saída",
        "",
        "`print(v)` imprime qualquer valor conforme o tipo — inteiros, números, strings, bools, arrays, maps e structs:",
        "",
        "```cryo",
        "print(\"texto\");",
        "print(42);",
        "print(3.14);",
        "print(true);",
        "print([1, 2, 3]);",
        "```",
        "",
        "## Entrada",
        "",
        "`input(prompt)` lê uma linha do terminal e devolve `string`:",
        "",
        "```cryo",
        "string nome = input(\"Seu nome: \");",
        "print(\"Ola, \" + nome);",
        "```",
        "",
        "> `input()` é sinalizado como `untrusted-input` (BAIXO) pela [auditoria estática](#/seguranca).",
        "",
        "## O corpo `={`",
        "",
        "Uma peculiaridade do Cryo: o corpo de funções, `tool` e `skill` abre com **`={`** (o lexer trata `={` como um único token). O restante da linguagem usa `{ }` normalmente (em `if`, `for`, structs, etc.).",
        "",
        "```cryo",
        "fn dobro(int x) -> int ={    // corpo abre com ={",
        "    return x * 2;",
        "}",
        "```"
      )
    },
    {
      slug: "tipos", group: "A linguagem Cryo", title: "Tipos",
      lead: "Os tipos primitivos, compostos e opcionais do Cryo.",
      body: md(
        "## Primitivos",
        "",
        "| Tipo | Descrição | Exemplo |",
        "|---|---|---|",
        "| `int` | Inteiro de 64 bits com sinal | `42`, `0xFF`, `1_000` |",
        "| `number` | Ponto flutuante (double) | `3.14`, `19.90` |",
        "| `string` | Texto UTF-8 dinâmico | `\"Ola\"` |",
        "| `bool` | Booleano | `true`, `false` |",
        "| `void` | Ausência de valor (retorno de função) | — |",
        "",
        "## Literais numéricos",
        "",
        "O lexer aceita prefixos de base e separadores de milhar com `_`:",
        "",
        "```cryo",
        "int hex   = 0xFF;        // hexadecimal  -> 255",
        "int bin   = 0b1010;      // binário      -> 10",
        "int oct   = 0o17;        // octal        -> 15",
        "int grand = 1_000_000;   // underscore como separador",
        "number f  = 3.14;",
        "```",
        "",
        "## Compostos",
        "",
        "| Tipo | Descrição | Página |",
        "|---|---|---|",
        "| `T[]` | Array de `T` | [Arrays](#/arrays) |",
        "| `map<K, V>` | Mapa de `K` para `V` | [Mapas](#/mapas) |",
        "| `struct` | Registro de campos nomeados | [Structs & Enums](#/structs-enums) |",
        "| `enum` | Conjunto de constantes nomeadas | [Structs & Enums](#/structs-enums) |",
        "| `T?` | Opcional (pode ser `null`) | [Opcionais](#/opcionais) |",
        "| `future<T>` | Resultado pendente de `spawn` | [Concorrência](#/concorrencia) |",
        "",
        "## Conversões",
        "",
        "Builtins convertem entre tipos primitivos:",
        "",
        "```cryo",
        "string s = to_string(42);      // \"42\"",
        "int    i = to_int(\"7\");        // 7",
        "number n = to_number(\"3.14\");  // 3.14",
        "```",
        "",
        "## Aritmética mista int/number",
        "",
        "Ao misturar `int` e `number`, o inteiro é promovido a ponto flutuante antes da operação — o resultado é `number`:",
        "",
        "```cryo",
        "int    qtd   = 2;",
        "number frete = 12.50 + qtd * 6.00;   // qtd vira float -> 24.50",
        "```"
      )
    },
    {
      slug: "funcoes", group: "A linguagem Cryo", title: "Funções",
      lead: "Funções tipadas, recursão e o tipo de retorno com `->`.",
      body: md(
        "## Declaração",
        "",
        "A forma geral é `fn nome(params) -> tipoRetorno ={ ... }`:",
        "",
        "```cryo",
        "fn areaCirculo(number raio) -> number ={",
        "    return 3.14159 * raio * raio;",
        "}",
        "",
        "fn ehPar(int n) -> bool ={",
        "    return (n & 1) == 0;",
        "}",
        "```",
        "",
        "- Cada parâmetro é `tipo nome`, separados por vírgula.",
        "- O tipo de retorno vem após `->`.",
        "- O corpo abre com **`={`** (veja [Sintaxe básica](#/sintaxe)).",
        "",
        "## Recursão",
        "",
        "Funções podem chamar a si mesmas:",
        "",
        "```cryo",
        "fn fatorial(int n) -> int ={",
        "    if (n <= 1) { return 1; }",
        "    return n * fatorial(n - 1);",
        "}",
        "",
        "fn fib(int n) -> int ={",
        "    if (n < 2) { return n; }",
        "    return fib(n - 1) + fib(n - 2);",
        "}",
        "```",
        "",
        "## Retornando compostos",
        "",
        "Funções podem devolver arrays, maps, structs e opcionais:",
        "",
        "```cryo",
        "struct Produto { string nome; number preco; }",
        "",
        "fn buscar(string sku) -> Produto ={",
        "    if (sku == \"SKU-1\") { return new Produto { nome: \"Fone\", preco: 129.90 }; }",
        "    return new Produto { nome: \"Desconhecido\", preco: 0.0 };",
        "}",
        "",
        "// retorno opcional: pode devolver null",
        "fn preco(map<string, number> tab, string k) -> number? ={",
        "    if (has(tab, k)) { return tab[k]; }",
        "    return null;",
        "}",
        "```"
      )
    },
    {
      slug: "controle-de-fluxo", group: "A linguagem Cryo", title: "Controle de fluxo",
      lead: "`if`/`else`, `while`, `do/while`, os dois tipos de `for`, `switch` e `break`/`continue`.",
      body: md(
        "## if / else",
        "",
        "```cryo",
        "if (n < 0) {",
        "    print(\"negativo\");",
        "} else if (n == 0) {",
        "    print(\"zero\");",
        "} else {",
        "    print(\"positivo\");",
        "}",
        "```",
        "",
        "## while e do/while",
        "",
        "```cryo",
        "int i = 0;",
        "while (i < 10) { i++; }",
        "",
        "do {",
        "    i--;",
        "} while (i > 0);",
        "```",
        "",
        "## for clássico",
        "",
        "```cryo",
        "int soma = 0;",
        "for (int i = 0; i < 5; i++) {",
        "    soma += i;",
        "}",
        "```",
        "",
        "## for-each",
        "",
        "Itera sobre arrays e sobre `keys(map)`:",
        "",
        "```cryo",
        "int[] valores = [10, 20, 30];",
        "for (int v in valores) {",
        "    print(v);",
        "}",
        "",
        "map<string, int> estoque = {\"maca\": 10, \"pera\": 5};",
        "for (string k in keys(estoque)) {",
        "    print(estoque[k]);",
        "}",
        "```",
        "",
        "## switch / case / default",
        "",
        "`case` sem `break`/`return` cai para o próximo (fall-through), útil para agrupar:",
        "",
        "```cryo",
        "fn nomeDia(int d) -> string ={",
        "    switch (d) {",
        "        case 0:",
        "        case 6:",
        "            return \"fim de semana\";",
        "        case 1: return \"segunda\";",
        "        default:",
        "            return \"dia util\";",
        "    }",
        "}",
        "```",
        "",
        "## break / continue",
        "",
        "```cryo",
        "for (int i = 2; i < n; i++) {",
        "    if (n % i != 0) { continue; }   // pula para a próxima iteração",
        "    return i;                        // primeiro divisor",
        "}",
        "```",
        "",
        "## Operador ternário",
        "",
        "```cryo",
        "bool grande = soma > 100 ? true : false;",
        "```"
      )
    },
    {
      slug: "operadores", group: "A linguagem Cryo", title: "Operadores",
      lead: "Aritméticos, comparação, lógicos, bit a bit e atribuições compostas.",
      body: md(
        "## Aritméticos",
        "",
        "`+`  `-`  `*`  `/`  `%` — além de `++` e `--`. Em modo seguro, `+ - *` passam por checagem de overflow e `/ %` abortam na divisão por zero (veja [Segurança](#/seguranca)).",
        "",
        "## Comparação e lógicos",
        "",
        "| Comparação | Lógicos |",
        "|---|---|",
        "| `==` `!=` `<` `>` `<=` `>=` | `&&` `||` `!` |",
        "",
        "## Bit a bit",
        "",
        "```cryo",
        "int empacotado = (0x12 << 8) | 0x34;   // -> 0x1234",
        "bool par = (n & 1) == 0;",
        "int inverso = ~mask;",
        "```",
        "",
        "Operadores: `&` (and), `|` (or), `^` (xor), `~` (not), `<<` (shift left), `>>` (shift right).",
        "",
        "## Atribuições compostas",
        "",
        "Cada operador binário tem sua forma composta:",
        "",
        "```cryo",
        "x += 1;   x -= 1;   x *= 2;   x /= 2;   x %= 3;",
        "x &= m;   x |= m;   x ^= m;   x <<= 2;  x >>= 2;",
        "```",
        "",
        "## Null-coalescing",
        "",
        "`??` devolve o lado esquerdo se não for `null`, senão o direito:",
        "",
        "```cryo",
        "string leitura = null;",
        "string resultado = leitura ?? \"sem leitura\";   // \"sem leitura\"",
        "```",
        "",
        "Veja também o unwrap `x!` em [Opcionais](#/opcionais)."
      )
    },
    {
      slug: "structs-enums", group: "A linguagem Cryo", title: "Structs & Enums",
      lead: "Registros de campos nomeados e conjuntos de constantes.",
      body: md(
        "## Structs",
        "",
        "Um `struct` agrupa campos tipados:",
        "",
        "```cryo",
        "struct Sensor {",
        "    int    id;",
        "    string nome;",
        "    number valor;",
        "    bool   ativo;",
        "}",
        "```",
        "",
        "Construa com `new` e acesse campos com `.`:",
        "",
        "```cryo",
        "Sensor s1 = new Sensor {",
        "    id:    1,",
        "    nome:  \"Temperatura\",",
        "    valor: 36.6,",
        "    ativo: true",
        "};",
        "string nome = s1.nome;   // \"Temperatura\"",
        "```",
        "",
        "> Structs podem ser **serializados para JSON** com `json_encode` e reconstruídos com `json_decode ... as T`. Veja [JSON nativo](#/json).",
        "",
        "## Enums",
        "",
        "Um `enum` define constantes nomeadas. Os membros são referenciados como `Enum_MEMBRO`:",
        "",
        "```cryo",
        "enum Nivel { BAIXO, MEDIO, ALTO }",
        "",
        "fn descNivel(Nivel nv) -> string ={",
        "    switch (nv) {",
        "        case Nivel_ALTO:  return \"ALTO\";",
        "        case Nivel_MEDIO: return \"MEDIO\";",
        "        default:          return \"BAIXO\";",
        "    }",
        "}",
        "",
        "string txt = descNivel(Nivel_ALTO);",
        "```",
        "",
        "## Cobertura por backend",
        "",
        "Structs funcionam nos quatro backends (com restrições no `asm`); enums estão em go/c/pyro. Veja a [matriz de backends](#/backends)."
      )
    },
    {
      slug: "arrays", group: "A linguagem Cryo", title: "Arrays",
      lead: "Listas homogêneas com literais, indexação, `push` e iteração.",
      body: md(
        "## Literais e indexação",
        "",
        "```cryo",
        "int[] nums = [3, 1, 4, 1, 5, 9];",
        "int primeiro = nums[0];",
        "nums[0] = 100;               // atribuição por índice",
        "```",
        "",
        "## len e push",
        "",
        "```cryo",
        "nums.push(10);               // adiciona ao final",
        "int n = len(nums);           // tamanho atual",
        "```",
        "",
        "## Iteração",
        "",
        "```cryo",
        "int total = 0;",
        "int maior = 0;",
        "for (int v in nums) {",
        "    total += v;",
        "    if (v > maior) { maior = v; }",
        "}",
        "```",
        "",
        "## Arrays de qualquer tipo",
        "",
        "O elemento pode ser qualquer tipo — inclusive `struct` ou `future<T>`:",
        "",
        "```cryo",
        "future<int>[] pendentes = [];",
        "for (int id in ids) {",
        "    pendentes.push(spawn tarefa(id));",
        "}",
        "```",
        "",
        "> **Bounds-checking:** no backend Go, o acesso fora dos limites é garantido pela própria runtime; no backend C, pelo runtime `cryo_runtime.c`."
      )
    },
    {
      slug: "mapas", group: "A linguagem Cryo", title: "Mapas",
      lead: "`map<K, V>` — dicionários com literais `{k: v}`, acesso `m[k]` e os builtins `has`, `keys`, `remove`.",
      body: md(
        "> Mapas estão disponíveis no **backend go**. Veja a [matriz de backends](#/backends).",
        "",
        "## Literais e acesso",
        "",
        "```cryo",
        "map<string, number> precos = {",
        "    \"SKU-1\": 19.90,",
        "    \"SKU-2\": 49.90,",
        "    \"SKU-3\": 5.00",
        "};",
        "precos[\"SKU-4\"] = 99.90;      // insere/atualiza",
        "number p = precos[\"SKU-2\"];",
        "```",
        "",
        "## has, keys, remove",
        "",
        "| Builtin | Efeito |",
        "|---|---|",
        "| `has(m, k)` | `true` se a chave existe |",
        "| `keys(m)` | array com as chaves |",
        "| `remove(m, k)` | remove a chave |",
        "",
        "```cryo",
        "number soma = 0.0;",
        "for (string k in keys(precos)) {",
        "    soma += precos[k];",
        "}",
        "bool tem = has(precos, \"SKU-1\");",
        "```",
        "",
        "## Lookup seguro com opcional",
        "",
        "Combine mapas com [opcionais](#/opcionais) para evitar erros por chave ausente:",
        "",
        "```cryo",
        "fn buscarPreco(map<string, number> tabela, string sku) -> number? ={",
        "    if (has(tabela, sku)) { return tabela[sku]; }",
        "    return null;",
        "}",
        "",
        "number? achado = buscarPreco(precos, \"SKU-2\");",
        "number final = achado ?? 0.0;    // fallback se ausente",
        "```"
      )
    },
    {
      slug: "opcionais", group: "A linguagem Cryo", title: "Opcionais & null-safety",
      lead: "O tipo `T?` modela ausência de valor com segurança: `??`, unwrap `x!` e comparação `x == null`.",
      body: md(
        "> Opcionais estão disponíveis no **backend go**.",
        "",
        "## Declarando opcionais",
        "",
        "Anexe `?` ao tipo para permitir `null`:",
        "",
        "```cryo",
        "number? achado = null;",
        "string? leitura = obterLeitura();",
        "```",
        "",
        "## Fornecendo um padrão com ??",
        "",
        "```cryo",
        "number final = achado ?? 0.0;       // usa 0.0 se achado for null",
        "string txt   = leitura ?? \"vazio\";",
        "```",
        "",
        "## Comparando com null",
        "",
        "```cryo",
        "if (achado == null) {",
        "    print(\"nao encontrado\");",
        "}",
        "```",
        "",
        "## Unwrap com !",
        "",
        "`x!` extrai o valor de um opcional que você sabe não ser nulo:",
        "",
        "```cryo",
        "number valor = achado!;   // assume que achado != null",
        "```",
        "",
        "> Prefira `??` ou a checagem `== null` quando houver dúvida — o unwrap parte do princípio de que o valor existe."
      )
    },
    {
      slug: "json", group: "A linguagem Cryo", title: "JSON nativo",
      lead: "Serialização e desserialização tipada entre structs e JSON — a base para APIs e respostas de LLM.",
      body: md(
        "> JSON nativo está disponível no **backend go**.",
        "",
        "## json_encode",
        "",
        "Converte structs (e valores) em uma string JSON:",
        "",
        "```cryo",
        "struct Produto { string sku; string nome; number preco; bool ativo; }",
        "",
        "Produto p = new Produto { sku: \"SKU-2\", nome: \"Plano Pro\", preco: 49.90, ativo: true };",
        "string payload = json_encode(p);",
        "```",
        "",
        "## json_decode ... as T",
        "",
        "Reconstrói um struct tipado a partir de JSON — o caso típico de uma resposta de API ou LLM:",
        "",
        "```cryo",
        "struct RespostaAPI { bool ok; string mensagem; int total; }",
        "",
        "string entrada = \"{\\\"ok\\\": true, \\\"mensagem\\\": \\\"criado\\\", \\\"total\\\": 4}\";",
        "RespostaAPI r = json_decode(entrada) as RespostaAPI;",
        "",
        "print(r.ok);         // true",
        "print(r.mensagem);   // criado",
        "```",
        "",
        "## Relação com LLM",
        "",
        "A construção `llm(\"modelo\", prompt) as Tipo` usa o mesmo mecanismo de desserialização tipada por trás do **structured output**. Veja [schema & structured output](#/schema)."
      )
    },
    {
      slug: "erros", group: "A linguagem Cryo", title: "Tratamento de erros",
      lead: "`try` / `catch` / `finally`, `throw` e `assert`.",
      body: md(
        "## try / catch / finally",
        "",
        "```cryo",
        "string estado = \"ok\";",
        "try {",
        "    if (soma > 100) {",
        "        throw(\"soma grande demais\");",
        "    }",
        "} catch (string err) {",
        "    estado = \"capturado: \" + err;",
        "} finally {",
        "    print(\"sempre executa\");",
        "}",
        "```",
        "",
        "> No **backend Go**, `try/catch/finally` é traduzido para `func(){ defer/recover }()` e `throw(...)` vira `panic(...)`.",
        "",
        "## assert",
        "",
        "`assert` aborta a execução se a condição for falsa — parte do [modo seguro](#/seguranca):",
        "",
        "```cryo",
        "assert(fatorial(10) == 3628800, \"fatorial incorreto\");",
        "assert(empacotado == 0x1234);   // mensagem é opcional",
        "```",
        "",
        "## Segurança sempre ativa",
        "",
        "Mesmo sem `try`, o modo seguro protege contra estouro de inteiros e divisão por zero. Veja [Segurança de código](#/seguranca)."
      )
    },

    // =========================================================
    //  CONCORRÊNCIA & REDE
    // =========================================================
    {
      slug: "concorrencia", group: "Concorrência & rede", title: "Concorrência",
      lead: "`spawn` dispara trabalho concorrente e devolve um `future<T>`; `await` coleta o resultado. No backend Go, baixam para goroutines + canais.",
      body: md(
        "> Concorrência está disponível no **backend go**.",
        "",
        "## spawn / await",
        "",
        "```cryo",
        "fn tarefa(int id) -> int ={",
        "    sleep(30);",
        "    return id * id;",
        "}",
        "",
        "future<int> a = spawn tarefa(6);   // dispara",
        "future<int> b = spawn tarefa(7);",
        "int ra = await a;                   // coleta",
        "int rb = await b;",
        "print(ra + rb);                     // 36 + 49 = 85",
        "```",
        "",
        "## Fan-out: N tarefas em paralelo",
        "",
        "O padrão clássico de chamadas concorrentes (ex.: várias chamadas de LLM ao mesmo tempo):",
        "",
        "```cryo",
        "int[] ids = [1, 2, 3, 4, 5];",
        "future<int>[] pendentes = [];",
        "for (int id in ids) {",
        "    pendentes.push(spawn tarefa(id));   // dispara todas de uma vez",
        "}",
        "int soma = 0;",
        "for (future<int> f in pendentes) {",
        "    soma += await f;                    // coleta os resultados",
        "}",
        "```",
        "",
        "## sleep",
        "",
        "`sleep(ms)` pausa a execução pelo número de milissegundos indicado — útil para simular I/O e para *backoff*."
      )
    },
    {
      slug: "http", group: "Concorrência & rede", title: "HTTP",
      lead: "Cliente HTTP embutido para falar com APIs — de serviços SaaS a provedores de LLM.",
      body: md(
        "> HTTP está disponível no **backend go**.",
        "",
        "## http_get e http_post",
        "",
        "```cryo",
        "string corpo = http_get(\"http://example.com\");",
        "string resp  = http_post(\"https://api.exemplo.com/v1/itens\", payload);",
        "```",
        "",
        "Ambos devolvem o corpo da resposta como `string`. Sem rede disponível, retornam `\"\"` — o programa continua compilando e rodando.",
        "",
        "## Combinando com concorrência",
        "",
        "Dispare várias requisições em paralelo com [spawn/await](#/concorrencia):",
        "",
        "```cryo",
        "fn baixar(string url) -> string ={",
        "    return http_get(url);",
        "}",
        "",
        "future<string> f1 = spawn baixar(\"https://a.exemplo.com\");",
        "future<string> f2 = spawn baixar(\"https://b.exemplo.com\");",
        "string r1 = await f1;",
        "string r2 = await f2;",
        "```",
        "",
        "Para chamar um LLM real através de um gateway HTTP, veja [Conectando um LLM real](#/llm-real)."
      )
    },

    // =========================================================
    //  LLM & AGENTES
    // =========================================================
    {
      slug: "schema", group: "LLM & Agentes", title: "Schema & structured output",
      lead: "Declare um `schema` e receba respostas de LLM **tipadas e validadas** com `llm(...) as T`.",
      body: md(
        "> Recursos de LLM estão disponíveis no **backend go**. Roadmap SaaS/LLM completo (Fase 3).",
        "",
        "## Declarando um schema",
        "",
        "Um `schema` é um struct cujo **JSON Schema** é gerado nativamente em tempo de compilação:",
        "",
        "```cryo",
        "schema Fatura {",
        "    string   cliente;",
        "    number   total;",
        "    string[] itens;",
        "    bool     paga;",
        "}",
        "```",
        "",
        "## schema_of(T)",
        "",
        "Obtenha o JSON Schema de um tipo — pronto para enviar a um modelo:",
        "",
        "```cryo",
        "print(schema_of(Fatura));",
        "```",
        "",
        "## llm(...) as T — structured output",
        "",
        "A chamada `llm(\"modelo\", prompt) as Tipo` pede ao modelo uma resposta no formato do schema, **desserializa e valida** (com retry):",
        "",
        "```cryo",
        "string texto = \"Cliente Ana, total 149,90, itens: plano pro\";",
        "Fatura f = llm(\"gpt-x\", \"Extraia a fatura: \" + texto) as Fatura;",
        "print(f.cliente);",
        "print(f.total);",
        "```",
        "",
        "## Chamada crua (sem schema)",
        "",
        "Sem `as T`, `llm` devolve o texto da *completion*:",
        "",
        "```cryo",
        "string resposta = llm(\"gpt-x\", \"Resuma em uma frase: Cryo compila para Pyro\");",
        "```",
        "",
        "> O endpoint é plugável via `CRYO_LLM_URL` / `CRYO_LLM_KEY`. Sem endpoint, `llm(...)` retorna vazio — o programa compila e roda. Veja [Conectando um LLM real](#/llm-real)."
      )
    },
    {
      slug: "tools", group: "LLM & Agentes", title: "Tools",
      lead: "Uma `tool fn` é uma função exposta ao modelo. O schema dos parâmetros é derivado da assinatura automaticamente.",
      body: md(
        "## Declarando uma tool",
        "",
        "Prefixe `fn` com `tool`. O corpo é uma função Cryo normal:",
        "",
        "```cryo",
        "tool fn buscar_preco(string sku) -> number ={",
        "    if (sku == \"SKU-1\") { return 19.90; }",
        "    if (sku == \"SKU-2\") { return 49.90; }",
        "    return 0.0;",
        "}",
        "",
        "tool fn registrar_pagamento(string cliente, number valor) -> bool ={",
        "    return true;",
        "}",
        "```",
        "",
        "## Introspecção: tools() e tools_json()",
        "",
        "| Builtin | Devolve |",
        "|---|---|",
        "| `tools()` | array com os nomes das tools |",
        "| `tools_json()` | catálogo das tools em JSON (nome + schema dos parâmetros) |",
        "| `tool_get(nome)` | metadados de uma tool específica |",
        "",
        "```cryo",
        "print(tools());        // [\"buscar_preco\", \"registrar_pagamento\"]",
        "print(tools_json());   // catálogo JSON para enviar ao modelo",
        "```",
        "",
        "## Tools que retornam structs",
        "",
        "Uma tool pode devolver um `struct` — o runtime o serializa para JSON ao entregar o resultado ao modelo:",
        "",
        "```cryo",
        "struct Produto { string nome; number preco; }",
        "",
        "tool fn buscar_produto(string sku) -> Produto ={",
        "    if (sku == \"SKU-1\") { return new Produto { nome: \"Fone\", preco: 129.90 }; }",
        "    return new Produto { nome: \"Desconhecido\", preco: 0.0 };",
        "}",
        "```",
        "",
        "As tools ganham vida no [laço de agent](#/agent), onde o modelo decide quais chamar."
      )
    },
    {
      slug: "agent", group: "LLM & Agentes", title: "O laço de agent",
      lead: "`agent(...)` roda o ciclo de tool-calling: o modelo pede uma tool, o runtime **executa a função real** e devolve o resultado, repetindo até a resposta final.",
      body: md(
        "## Como funciona",
        "",
        "O contrato do endpoint é simples:",
        "",
        "```text",
        "POST {model, messages, tools}",
        "   -> {\"tool_call\": {\"name\": \"...\", \"arguments\": {...}}}   (o modelo pede uma tool)",
        "   -> {\"content\": \"resposta final\"}                        (encerra)",
        "```",
        "",
        "A cada `tool_call`, o runtime desempacota os argumentos, chama a `tool fn` correspondente, serializa o retorno e o devolve ao modelo — até vir um `content`.",
        "",
        "## Forma simples",
        "",
        "```cryo",
        "tool fn buscar_preco(string sku) -> number ={ /* ... */ }",
        "tool fn aplicar_desconto(number preco, number pct) -> number ={",
        "    return preco - (preco * pct / 100.0);",
        "}",
        "",
        "string resposta = agent(\"gpt-x\", \"Qual o preco do SKU-1 com 10% de desconto?\");",
        "print(resposta);",
        "```",
        "",
        "O modelo encadeia `buscar_preco` → `aplicar_desconto` → resposta final, sozinho.",
        "",
        "## Agente configurável",
        "",
        "`agent` aceita, opcionalmente, um **subconjunto de tools** (array de nomes) e um **limite de passos**:",
        "",
        "```cryo",
        "// agent(modelo, prompt, [tools...], maxPassos)",
        "string r = agent(\"gpt-x\",",
        "    \"Crie uma landing page e salve em build/landpage.html.\",",
        "    [\"montar_html\", \"salvar_pagina\", \"abrir_pagina\"], 5);",
        "```",
        "",
        "- **Subconjunto de tools** — restringe o que o agente pode chamar naquela invocação.",
        "- **maxPassos** — teto de iterações do laço (padrão: 8; qualquer valor ≤ 0 volta a 8).",
        "",
        "## Exemplo realista: e-commerce com 6 tools",
        "",
        "```cryo",
        "struct Produto { string nome; number preco; }",
        "",
        "tool fn buscar_cliente(string email) -> string ={ /* ... */ }",
        "tool fn buscar_produto(string sku) -> Produto ={ /* ... */ }",
        "tool fn verificar_estoque(string sku) -> int ={ /* ... */ }",
        "tool fn calcular_frete(string cep, int qtd) -> number ={ return 12.50 + qtd * 6.00; }",
        "tool fn aplicar_cupom(number total, string cupom) -> number ={",
        "    if (cupom == \"PROMO10\") { return total * 0.90; }",
        "    return total;",
        "}",
        "tool fn criar_pedido(string cliente, number total) -> string ={ return \"PED-1007\"; }",
        "",
        "string resposta = agent(\"gpt-x\",",
        "    \"Faca um pedido para ana@loja.com: 2 unidades do SKU-1, cupom PROMO10.\");",
        "```",
        "",
        "> O laço foi validado de ponta a ponta pelo protocolo OpenAI (tool-calling multi-passo). Veja como plugar um provedor real em [Conectando um LLM real](#/llm-real)."
      )
    },
    {
      slug: "llm-real", group: "LLM & Agentes", title: "Conectando um LLM real",
      lead: "O runtime fala um contrato simples. Um gateway incluído traduz esse contrato para a API de Chat Completions (OpenAI-compatível).",
      body: md(
        "## O contrato do runtime",
        "",
        "O Cryo envia `POST {model, prompt|messages, tools?}` e espera de volta:",
        "",
        "```json",
        "{ \"tool_call\": { \"name\": \"buscar_preco\", \"arguments\": { \"sku\": \"SKU-1\" } } }",
        "```",
        "",
        "ou, para encerrar:",
        "",
        "```json",
        "{ \"content\": \"O preco com desconto e R$ 17,91.\" }",
        "```",
        "",
        "O endpoint é configurado por variáveis de ambiente: `CRYO_LLM_URL` e `CRYO_LLM_KEY`.",
        "",
        "## O gateway incluído",
        "",
        "`burnout/scripts/llm_gateway.py` traduz o contrato do Cryo para a API de Chat Completions — compatível com OpenAI, Anthropic `/v1`, Groq, ou local (Ollama/LM Studio):",
        "",
        "```bash",
        "# terminal 1 — o gateway (com a sua chave)",
        "set OPENAI_API_KEY=sk-...",
        "set OPENAI_BASE_URL=https://api.openai.com/v1        # ou outro provedor",
        "python burnout/scripts/llm_gateway.py 8801",
        "```",
        "",
        "```bash",
        "# terminal 2 — o agente Cryo apontando para o gateway",
        "set CRYO_LLM_URL=http://127.0.0.1:8801",
        "python burnout/cryoc.py cryo/examples/example_agent.cryo --backend go --run",
        "```",
        "",
        "## Sem endpoint",
        "",
        "Sem `CRYO_LLM_URL`, `llm(...)` e `agent(...)` retornam vazio — mas o programa **compila e roda** normalmente. Isso mantém builds e testes independentes de rede."
      )
    },
    {
      slug: "skills", group: "LLM & Agentes", title: "Skills nativas",
      lead: "Skills de LLM são um construto da linguagem, **compiladas no binário** (sem arquivos `.md`), introspectáveis de forma compacta.",
      body: md(
        "> Skills estão disponíveis no **backend go**.",
        "",
        "## Declarando skills",
        "",
        "```cryo",
        "skill resumir {",
        "    desc:        \"Resume um texto em bullets objetivos\";",
        "    model:       \"gpt-x\";",
        "    temperature: 0.2;",
        "    max_tokens:  512;",
        "    tools:       [\"contar_palavras\"];",
        "}",
        "",
        "skill traduzir {",
        "    desc:  \"Traduz texto entre idiomas\";",
        "    model: \"gpt-x-mini\";",
        "    tools: [];",
        "}",
        "```",
        "",
        "## Introspecção",
        "",
        "| Builtin | Devolve |",
        "|---|---|",
        "| `skills()` | nomes de todas as skills |",
        "| `skill_get(n)` | o objeto `Skill` (campos `desc`, `model`, `config[...]`) |",
        "| `skill_has(n)` | `true` se a skill existe |",
        "| `skills_json()` | catálogo em JSON (sem markdown) |",
        "",
        "```cryo",
        "string[] nomes = skills();",
        "Skill s = skill_get(\"resumir\");",
        "print(s.desc);",
        "print(s.model);",
        "print(s.config[\"temperature\"]);",
        "print(skills_json());",
        "```",
        "",
        "Como as skills vivem no binário, não há arquivos soltos para versionar ou sincronizar — o catálogo é sempre coerente com o código compilado."
      )
    },
    {
      slug: "maquina", group: "LLM & Agentes", title: "Acesso à máquina",
      lead: "A camada Pyro expõe builtins `pyro_*` para tocar o sistema — executar comandos, ler o ambiente, escrever e abrir arquivos.",
      body: md(
        "> Builtins `pyro_*` estão disponíveis no **backend go**.",
        "",
        "## Builtins de sistema",
        "",
        "| Builtin | Efeito |",
        "|---|---|",
        "| `pyro_exec(cmd)` | executa um comando e devolve a saída (`string`) |",
        "| `pyro_env(n)` | lê a variável de ambiente `n` |",
        "| `pyro_args()` | argumentos da linha de comando (`string[]`) |",
        "| `pyro_time()` | timestamp atual (`int`) |",
        "| `pyro_read()` | lê uma linha da entrada |",
        "| `pyro_write_file(path, conteudo)` | escreve um arquivo; devolve `bool` |",
        "| `pyro_open(path)` | abre um arquivo/URL no app padrão do SO; devolve `bool` |",
        "| `pyro_exit(cod)` | encerra o processo com o código dado |",
        "",
        "## Exemplo",
        "",
        "```cryo",
        "string saida = pyro_exec(\"echo ola-da-maquina\");",
        "int agora    = pyro_time();",
        "print(agora > 0);",
        "```",
        "",
        "## Um agente que cria e abre uma página",
        "",
        "`pyro_write_file` e `pyro_open` permitem que um agente materialize um arquivo e o abra no navegador:",
        "",
        "```cryo",
        "tool fn salvar_pagina(string html, string caminho) -> bool ={",
        "    return pyro_write_file(caminho, html);",
        "}",
        "tool fn abrir_pagina(string caminho) -> bool ={",
        "    return pyro_open(caminho);   // start (Windows) / open (macOS) / xdg-open (Linux)",
        "}",
        "```",
        "",
        "> `pyro_open` seleciona o comando por `runtime.GOOS`: `cmd /c start` no Windows, `open` no macOS e `xdg-open` no Linux.",
        "",
        "> **Cuidado:** `pyro_exec` executa código arbitrário na máquina. A [auditoria estática](#/seguranca) sinaliza usos sensíveis; revise programas de origem não confiável antes de rodar."
      )
    },

    // =========================================================
    //  COMPILADOR BURNOUT
    // =========================================================
    {
      slug: "cli", group: "Compilador Burnout", title: "A CLI (cryoc)",
      lead: "`burnout/cryoc.py` é o ponto de entrada — compila, executa, inspeciona e audita.",
      body: md(
        "## Uso",
        "",
        "```bash",
        "python burnout/cryoc.py <arquivo.cryo> [opções]",
        "```",
        "",
        "## Opções",
        "",
        "| Opção | Efeito |",
        "|---|---|",
        "| `--backend {auto,go,c,asm,pyro,node}` | Escolhe o gerador; `auto` deduz o melhor pelo programa (padrão: `go`) |",
        "| `--run` | Compila e executa em seguida |",
        "| `-o, --output <arq>` | Caminho do artefato de saída |",
        "| `--abi {sysv,win64}` | ABI do backend asm (padrão por plataforma) |",
        "| `--unsafe` | Desliga a instrumentação de segurança |",
        "| `--audit` | Roda a auditoria estática (relatório + sugestão de backend) e **segue compilando** |",
        "| `--audit-only` | Roda a auditoria, imprime o relatório e **sai sem compilar** |",
        "| `--emit-only` | Só gera o fonte (`.pyro`/`.s`); não invoca o toolchain |",
        "| `--dis` | Desassembla o bytecode Pyro gerado |",
        "| `--tokens` | Imprime os tokens do lexer |",
        "| `--ast` | Imprime a AST |",
        "| `-v, --verbose` | Saída detalhada |",
        "| `--no-banner` | Oculta o banner |",
        "",
        "## Exemplos",
        "",
        "```bash",
        "# alvo Pyro (bytecode próprio) — gera .pyro e roda na VM",
        "python burnout/cryoc.py cryo/examples/example_bytecode.cryo --backend pyro --run",
        "",
        "# alvo Go (linguagem completa)",
        "python burnout/cryoc.py cryo/examples/example_saas.cryo --backend go --run",
        "",
        "# alvo C nativo / x86-64",
        "python burnout/cryoc.py cryo/examples/example_v4.cryo  --backend c",
        "python burnout/cryoc.py cryo/examples/example_asm.cryo --backend asm",
        "",
        "# inspeção e segurança",
        "python burnout/cryoc.py app.cryo --audit",
        "python burnout/cryoc.py app.cryo --backend pyro --dis",
        "```",
        "",
        "## Onde ficam os artefatos",
        "",
        "Sem `-o`, a saída vai para `build/` com o mesmo nome-base do fonte e a extensão do backend (`.go`, `.pyro`, `.c`, `.s`). Os fontes `.cryo` ficam separados dos artefatos gerados."
      )
    },
    {
      slug: "backends", group: "Compilador Burnout", title: "Backends",
      lead: "Quatro geradores de código a partir do mesmo AST. Cada um cobre um subconjunto da linguagem.",
      body: md(
        "## Matriz de cobertura",
        "",
        "| Recurso | Go (base) | Node | C | asm | Pyro |",
        "|---|:---:|:---:|:---:|:---:|:---:|",
        "| int/bool, aritmética, bitwise | ✅ | ✅ | ✅ | ✅ | ✅ |",
        "| funções, recursão, if/while/for/switch | ✅ | ✅ | ✅ | ✅ | ✅ |",
        "| break/continue/assert | ✅ | ✅ | ✅ | ✅ | ✅ |",
        "| number (double), string dinâmica | ✅ | ✅ | ✅ | ❌ | ✅ |",
        "| arrays, enum | ✅ | ✅ | ✅ | ❌ | ✅ |",
        "| builtins de matemática/conversão/string | ✅ | ✅ | ✅ (math) | ❌ | ✅ (NATIVE) |",
        "| struct | ✅ | ✅ | ✅ | ✅¹ | ✅ |",
        "| try/catch/finally | ✅² | ✅ | ✅ | ❌ | ❌ |",
        "| map `map<K,V>` | ✅ | ✅ | ❌ | ❌ | ✅ |",
        "| opcionais `T?` / null-safety | ✅ | ✅ | ❌ | ❌ | ❌ |",
        "| JSON nativo | ✅ | ✅ | ❌ | ❌ | ❌ |",
        "| concorrência, HTTP, LLM, skills, `pyro_*` | ✅ | ❌ | ❌ | ❌ | ❌ |",
        "| blocos estrangeiros | `>Go(...)` | `>Node(...)` | `>C(...)` | ❌ | ❌ |",
        "",
        "¹ O backend `asm` suporta **retorno de struct em registrador** (campos `int`/`bool`), conforme a classificação de cada ABI. Structs grandes ou por valor apontam para `--backend c`.",
        "",
        "² No Go, `try/catch/finally` vira `func(){ defer/recover }()`; `throw` vira `panic`.",
        "",
        "## Backend go (padrão)",
        "",
        "O mais completo — a rota SaaS/LLM. Requer apenas Go instalado; os helpers de segurança (`cryoOr`, `cryoAssert`, `cryoAddOvf`…) são embutidos no próprio `.go`, sem runtime externo.",
        "",
        "## Backend pyro",
        "",
        "Gera o **bytecode próprio** `.pyro`, executado pela VM Pyro. Cobre o núcleo da linguagem (int/number/bool/string, funções, controle de fluxo, aritmética/bitwise/lógica, arrays/maps/structs, print e assert) **mais enums e a stdlib** — matemática, conversões (`to_string`/`to_int`/`to_number`), as funções de string e `remove()`, executados pela instrução `NATIVE` da ISA. Veja [a linguagem-alvo Pyro](#/pyro-formato).",
        "",
        "## Backend node",
        "",
        "Compila o **núcleo do Cryo para JavaScript** (CommonJS) e executa com o `node` — sem etapa de build. Cobre escalares, strings, bool, arrays, maps (objetos), structs (objetos), enums, funções, todo o controle de fluxo, operadores, `print`/`assert`, JSON, e **blocos/libraries de JavaScript** (`>Node( ... )` / `>JS( ... )` e `library >node fs<` → `require`).",
        "",
        "```bash",
        "python burnout/cryoc.py app.cryo --backend node --run",
        "```",
        "",
        "O backend faz **inferência de tipos** para respeitar a semântica do Cryo: `int / int` gera divisão inteira (trunca para zero), enquanto envolver `number` gera divisão de ponto flutuante; e o acesso indexado a **arrays e strings** (não a maps) recebe **bounds-check** em modo seguro — inclusive em leitura/escrita **aninhada** (`m[i][j]` vira `cryoSetIndex(cryoIndex(m, i), j, v)`, checando os dois índices). A divisão por zero também aborta.",
        "",
        "> Recursos de LLM/agente, concorrência (`spawn`/`await`) e acesso à máquina (`pyro_*`) **não** são cobertos no backend node — use `--backend go`. O gerador emite um erro claro nesses casos. Não há inteiro de 64 bits (números são `double` do JavaScript).",
        "",
        "## Backends c e asm",
        "",
        "Geram C nativo e x86-64 (duas ABIs: System V e Win64). O backend `asm` emite erro claro apontando `--backend c` quando encontra um recurso fora do subconjunto nativo. Ambos usam o runtime `burnout/runtime/cryo_runtime.c`.",
        "",
        "## Seleção automática (--backend auto)",
        "",
        "Com `--backend auto`, o compilador **analisa o programa** e escolhe o backend ideal — otimizando o processamento ao evitar toolchains desnecessários:",
        "",
        "```bash",
        "python burnout/cryoc.py app.cryo --backend auto --run",
        "# → backend automático: pyro  (núcleo puro — bytecode Pyro, mais leve)",
        "```",
        "",
        "A escolha segue as capacidades exigidas pelo código:",
        "",
        "| O programa usa... | Backend escolhido |",
        "|---|---|",
        "| Apenas o núcleo (escalares, arrays, maps, structs, funções, fluxo) | `pyro` (mais leve) |",
        "| enum, `try/catch`, opcionais, JSON, `to_string`/math, concorrência, LLM, máquina | `go` |",
        "| Bloco `>Node( ... )` | `node` |",
        "| Bloco `>C( ... )` | `c` |",
        "| Bloco `>Go( ... )` | `go` |",
        "",
        "A análise é **conservadora**: só recomenda um backend que cobre todos os recursos usados; na dúvida, usa `go` (superconjunto). Se ainda assim o backend escolhido falhar, o compilador **recompila em `go`** automaticamente. Também disponível na API: `burnout.select_backend(ast)` e `burnout.compile_source(src, backend=\"auto\")`.",
        "",
        "## Interop (blocos estrangeiros)",
        "",
        "Blocos `>Go( ... )` (alvo go) e `>C( ... )` (alvo c) embutem código nativo, e `library >lang nome<` puxa dependências. Um bloco só é aceito se a sua linguagem foi importada com `import >Lang<`. Veja a página dedicada: [Blocos estrangeiros & libraries](#/estrangeiros)."
      )
    },
    {
      slug: "estrangeiros", group: "Compilador Burnout", title: "Blocos estrangeiros & libraries",
      lead: "Embuta código nativo (Go/C) e importe bibliotecas — de forma **verificada**: um bloco só compila se a sua linguagem foi importada no programa.",
      body: md(
        "## A regra: importar antes de usar",
        "",
        "Cada linguagem estrangeira precisa ser habilitada com `import >Lang<`. Um bloco `>Lang( ... )` só é aceito se `Lang` tiver sido importada — caso contrário o compilador rejeita com `[Erro Estrangeiro]`, **antes** de gerar qualquer código.",
        "",
        "```cryo",
        "import >C<               // habilita blocos e libraries C",
        "",
        ">C(",
        "    printf(\"ola do C\\n\");",
        ")",
        "```",
        "",
        "Sem o `import >C<`, o mesmo programa falha:",
        "",
        "```text",
        "[Erro Estrangeiro] bloco estrangeiro >C( ... ) usado sem importar a",
        "linguagem. Adicione 'import >C<' antes de usar blocos C.",
        "```",
        "",
        "> A verificação é **semântica e independente do backend**: roda depois do parse e antes da geração de código, tanto pela CLI quanto pela API `import burnout`.",
        "",
        "## Um bloco por linguagem, por backend",
        "",
        "Cada backend emite **apenas os blocos da sua própria linguagem**; os demais são omitidos com um comentário. Isso deixa um mesmo `.cryo` portável entre alvos:",
        "",
        "```cryo",
        "import >Go<",
        "import >C<",
        "",
        ">Go(   fmt.Println(\"[go] daqui\") )    // emitido no backend go",
        ">Node( console.log(\"[node] daqui\") ) // emitido no backend node",
        ">C(    printf(\"[c] daqui\\n\");   )    // emitido no backend c",
        "```",
        "",
        "| Backend | `>Go(...)` | `>Node(...)` | `>C(...)` |",
        "|---|:---:|:---:|:---:|",
        "| `go` | emitido | omitido | omitido |",
        "| `node` | omitido | emitido | omitido |",
        "| `c` | omitido | omitido | emitido |",
        "| `asm` / `pyro` | erro | erro | erro |",
        "",
        "## Libraries",
        "",
        "Uma `library` pertence a uma linguagem importada e vira uma **dependência real** no backend correspondente:",
        "",
        "| Declaração | Vira | No backend |",
        "|---|---|---|",
        "| `library >go fmt<` | `import \"fmt\"` | go |",
        "| `library >go net/http<` | `import \"net/http\"` | go |",
        "| `library >node fs<` | `const fs = require(\"fs\")` | node |",
        "| `library >c math<` | `#include <math.h>` | c |",
        "",
        "### Formas de escrever",
        "",
        "```cryo",
        "library >go fmt<      // qualificada por espaço",
        "library >go:fmt<      // qualificada por dois-pontos",
        "library >math<        // não qualificada: infere a linguagem",
        "```",
        "",
        "A forma **não qualificada** (`library >nome<`) infere a linguagem quando há exatamente uma importada. Com duas ou mais linguagens importadas ela é ambígua e o compilador pede a qualificação:",
        "",
        "```text",
        "[Erro Estrangeiro] library >math< é ambígua: várias linguagens",
        "importadas (c, go). Qualifique com 'library >LANG math<'.",
        "```",
        "",
        "## Exemplo completo e portável",
        "",
        "```cryo",
        "import >Go<",
        "import >C<",
        "library >go fmt<     // backend Go  ->  import \"fmt\"",
        "library >c math<     // backend C   ->  #include <math.h>",
        "",
        "number lado = 4.0;",
        "print(\"area (Cryo):\");",
        "print(lado * lado);",
        "",
        ">Go(",
        "    fmt.Println(\"[go] fmt veio da library\")",
        ")",
        ">C(",
        "    printf(\"[c] sqrt(2) = %.5f\\n\", sqrt(2.0));",
        ")",
        "```",
        "",
        "```bash",
        "python burnout/cryoc.py cryo/examples/example_foreign.cryo --backend go --run",
        "python burnout/cryoc.py cryo/examples/example_foreign.cryo --backend c  --run",
        "```",
        "",
        "Veja [`example_foreign.cryo`](#/exemplos) na lista de exemplos.",
        "",
        "## Verificação também pela biblioteca",
        "",
        "A API programática aplica a mesma regra e expõe a exceção:",
        "",
        "```python",
        "import burnout",
        "",
        "try:",
        "    burnout.compile_source('>Go( fmt.Println(1) )', backend='go')",
        "except burnout.ForeignError as e:",
        "    print('rejeitado:', e)",
        "",
        "burnout.collect_imports(burnout.parse_ast('import >go<'))  # {'go'}",
        "```",
        "",
        "## Segurança",
        "",
        "> A verificação garante que a **linguagem foi declarada** — não que o código embutido seja seguro. Blocos estrangeiros continuam sendo código não verificado pelo compilador; a [auditoria](#/seguranca) (`--audit`) os classifica como **ALTO**. Trate código estrangeiro (e libraries) como dependências confiáveis e revise-as."
      )
    },
    {
      slug: "seguranca", group: "Compilador Burnout", title: "Segurança de código",
      lead: "O modo seguro (padrão) instrumenta o código gerado; a auditoria estática (`--audit`) inspeciona a AST antes de compilar.",
      body: md(
        "## Modo seguro (padrão)",
        "",
        "Ativo por padrão; desligue com `--unsafe`. Vale, no que se aplica, para os três backends nativos:",
        "",
        "- **Overflow de inteiros** — `+ - *` passam por helpers que abortam em estouro (`cryoAddOvf`/`cryoSubOvf`/`cryoMulOvf` no Go; `cryo_add_ovf`/… via `__builtin_*_overflow` no C).",
        "- **Divisão/módulo por zero** — `/` e `%` sempre protegidos (`cryoIDivChk`/`cryoIModChk` no Go, runtime no C, e na VM Pyro). A **divisão** `INT64_MIN / -1` aborta em todos esses backends (resultado não representável); o **módulo** `INT64_MIN % -1` devolve `0` (bem-definido).",
        "- **`assert(cond)` / `assert(cond, \"msg\")`** — aborta se a condição falhar.",
        "- **Bounds-checking de arrays** — garantido pelo Go nativamente; no C via runtime.",
        "- **Endurecimento do binário (backend C)** — `-fstack-protector-strong`, `-D_FORTIFY_SOURCE=2`, `-Wformat-security`.",
        "",
        "## Blocos unsafe / safe",
        "",
        "Desligue a instrumentação localmente para hot paths auditados — e reative com `safe` dentro de um contexto `unsafe`:",
        "",
        "```cryo",
        "int rapido = 0;",
        "unsafe {",
        "    rapido = soma * 2;   // sem checagem de overflow aqui",
        "}",
        "```",
        "",
        "## Auditoria estática (--audit)",
        "",
        "Percorre a AST e classifica achados em **ALTO / MÉDIO / BAIXO**:",
        "",
        "| Achado | Nível | O que é |",
        "|---|---|---|",
        "| `foreign-block` | ALTO | blocos `>C(...)` embutem código não verificado |",
        "| `div-by-zero` | ALTO | divisão por literal zero |",
        "| `command-exec` | ALTO | `pyro_exec()` executa comando de shell arbitrário |",
        "| `unsafe-block` | MÉDIO | desativa a instrumentação |",
        "| `file-write` / `shell-open` | MÉDIO | `pyro_write_file()` / `pyro_open()` tocam disco/SO |",
        "| `net-egress` | MÉDIO | `http_get()` / `http_post()` (risco de SSRF) |",
        "| `external-lib` / `foreign-import` | BAIXO | dependências externas |",
        "| `llm-egress` | BAIXO | `llm()` / `agent()` enviam dados a endpoint externo |",
        "| `untrusted-input` | BAIXO | uso de `input()` |",
        "",
        "```bash",
        "python burnout/cryoc.py app.cryo --audit",
        "```",
        "",
        "Se houver qualquer achado de nível ALTO, a auditoria o registra na saída de erro. Duas formas de rodar: `--audit` imprime o relatório e **segue compilando**; `--audit-only` imprime o relatório e **sai** (útil em CI).",
        "",
        "### Sugestão de backend",
        "",
        "A auditoria também confere se o backend escolhido cobre os recursos usados. Se não cobrir (um recurso exclusivo, ou um bloco estrangeiro que ele não emite), sugere `--backend auto`:",
        "",
        "```text",
        "$ python burnout/cryoc.py app.cryo --backend c --audit",
        "[Auditoria] O backend 'c' não cobre recursos [map].",
        "            Use --backend auto (escolheria 'pyro') ou --backend pyro.",
        "```",
        "",
        "Disponível na API como `burnout.missing_capabilities(ast, backend)`."
      )
    },

    {
      slug: "biblioteca", group: "Compilador Burnout", title: "Burnout como biblioteca",
      lead: "Além da CLI, o Burnout é um **pacote Python importável** — chame o compilador direto dos seus projetos.",
      body: md(
        "## Instalação",
        "",
        "Instale em modo editável a partir da pasta `burnout` (dentro do monorepo Cryo, com `../cryo` e `../pyro` ao lado):",
        "",
        "```bash",
        "cd burnout",
        "pip install -e .",
        "```",
        "",
        "Isso registra o pacote `burnout` e o console-script `cryoc`.",
        "",
        "## Compilando a partir de código",
        "",
        "```python",
        "import burnout",
        "",
        "# string .cryo -> código-alvo (str para go/c/asm, bytes para pyro)",
        "go_src = burnout.compile_source('print(\"ola\");', backend=\"go\")",
        "bc     = burnout.compile_source('print(1 + 2);', backend=\"pyro\")",
        "",
        "# compila um arquivo e (opcionalmente) executa",
        "burnout.compile_file(\"app.cryo\", backend=\"pyro\", run=True)",
        "burnout.run(\"app.cryo\", backend=\"go\")   # atalho de compile_file(run=True)",
        "```",
        "",
        "## Front-end e bytecode",
        "",
        "```python",
        "toks = burnout.tokenize(open(\"app.cryo\").read())   # lista de tokens",
        "ast  = burnout.parse_ast(open(\"app.cryo\").read())  # AST (Program)",
        "",
        "# desmonta um .pyro já gerado",
        "print(burnout.disassemble(open(\"build/app.pyro\", \"rb\").read()))",
        "```",
        "",
        "## API pública",
        "",
        "| Símbolo | Descrição |",
        "|---|---|",
        "| `compile_source(src, backend='go', safe=True, abi=None)` | compila uma string; devolve str (go/c/asm) ou bytes (pyro) |",
        "| `compile_file(path, backend='go', run=False, ...)` | compila um arquivo; opcionalmente monta/executa |",
        "| `run(path, backend='go', **kw)` | atalho de `compile_file(run=True)` |",
        "| `parse_ast(src)` / `tokenize(src)` | front-end CRYO |",
        "| `select_backend(ast)` | backend recomendado + motivo (usado por `backend=\"auto\"`) |",
        "| `disassemble(bytes)` | listing legível de um `.pyro` |",
        "| `default_abi()` | ABI padrão da plataforma (backend asm) |",
        "| `BACKENDS` | `('go', 'pyro', 'c', 'asm', 'node')` |",
        "| `__version__` | versão do compilador |",
        "",
        "Exceções: `LexerError`, `ParseError`, `CodeGenError`, `CodeGenGoError`, `CodeGenAsmError`, `CodeGenPyroError`.",
        "",
        "## Como módulo executável",
        "",
        "A mesma CLI do `cryoc.py`, via `-m`:",
        "",
        "```bash",
        "python -m burnout app.cryo --backend go --run",
        "cryoc app.cryo --run        # console-script instalado pelo pip",
        "```",
        "",
        "> **Windows:** se o `pip install -e .` falhar ao escrever `Scripts\\cryoc.exe` (arquivo em uso), o pacote continua importável — use `python -m burnout`, ou repita a instalação com os terminais fechados."
      )
    },

    // =========================================================
    //  PYRO — BYTECODE & VM
    // =========================================================
    {
      slug: "pyro-formato", group: "Pyro — bytecode & VM", title: "Pyro: o formato .pyro",
      lead: "O `.pyro` é a linguagem-alvo própria: um bytecode binário com ISA inventada aqui — não é x86, nem Go, nem C.",
      body: md(
        "O modelo de execução é uma **máquina de pilha**: operandos vivem numa pilha e cada função tem seu próprio quadro de variáveis locais.",
        "",
        "## Layout do arquivo (little-endian)",
        "",
        "```text",
        "magic     4    \"PYRO\"",
        "version   1    0x01",
        "flags     1    bit0 = seção de código codificada (XOR rolling)",
        "nconsts   u16",
        "consts    nconsts × [ tag(1) + payload ]",
        "              tag 1 int64   → 8 bytes",
        "              tag 2 float64 → 8 bytes",
        "              tag 3 string  → u16 len + bytes UTF-8",
        "              tag 4 bool    → 1 byte",
        "nfuncs    u16",
        "funcs     nfuncs × [ nameidx u16, entry u32, nparams u8, nlocals u16 ]",
        "entryfn   u16    índice da função 'main'",
        "codelen   u32",
        "code      codelen bytes   (decodificados na carga se flags bit0)",
        "```",
        "",
        "As constantes são deduplicadas; o `nameidx` de cada função referencia uma string no pool. As strings do pool ficam em claro — **apenas a seção `code` é codificada**.",
        "",
        "## Codificação da seção de código (flags bit0)",
        "",
        "Uma ofuscação leve (**não** é criptografia forte) por XOR *rolling*: chave inicial `0x5A`, atualizada a cada byte por `k = (k·31 + 7 + b) & 0xFF`, onde `b` é o byte em claro. A VM aplica o inverso ao carregar.",
        "",
        "Serve para tornar o `.pyro` opaco à leitura casual e compacto para distribuição; para sigilo real, cifre o artefato à parte.",
        "",
        "## Por que um bytecode próprio?",
        "",
        "- **Roda na máquina** via a VM Pyro (portável: um binário Go).",
        "- **Compacto e opaco** por natureza — bom para distribuição.",
        "- **Nativo do sistema** — instruções bem definidas, ótimas como dados de treino para agentes de IA.",
        "- **Próprio**, não derivado de x86/Go/C."
      )
    },
    {
      slug: "pyro-isa", group: "Pyro — bytecode & VM", title: "Pyro: conjunto de instruções",
      lead: "Cada instrução é 1 byte de opcode + operandos de tamanho fixo. Saltos são relativos ao fim da própria instrução.",
      body: md(
        "## Opcodes",
        "",
        "| Opcode | Hex | Operando | Efeito na pilha |",
        "|---|---|---|---|",
        "| `HALT` | 00 | — | encerra |",
        "| `CONST` | 01 | u16 idx | empilha `consts[idx]` |",
        "| `TRUE`/`FALSE`/`NULL` | 02/03/04 | — | empilha literal |",
        "| `POP` | 05 | — | descarta o topo |",
        "| `LOAD` | 06 | u16 slot | empilha `local[slot]` |",
        "| `STORE` | 07 | u16 slot | `local[slot] = pop()` |",
        "| `ADD`…`MOD` | 10–14 | — | `pop b, a` → empilha `a op b` |",
        "| `NEG` | 15 | — | nega o topo |",
        "| `BAND`…`SHR` | 16–1A | — | bit a bit (inteiros) |",
        "| `BNOT` | 1B | — | complemento de bits |",
        "| `EQ`…`GE` | 20–25 | — | comparação → bool |",
        "| `NOT` | 26 | — | negação lógica |",
        "| `JMP` | 30 | i16 rel | salto relativo |",
        "| `JMPF`/`JMPT` | 31/32 | i16 rel | `pop`; salta se falso/verdadeiro |",
        "| `CALL` | 40 | u16 fn, u8 argc | chama função (novo quadro) |",
        "| `RET` | 41 | — | retorna o topo ao chamador |",
        "| `PRINT` | 50 | — | imprime `pop()` conforme o tipo |",
        "| `ASSERT` | 51 | — | `pop cond, msg`; aborta se falso |",
        "| `PRINTLN` | 52 | — | imprime linha vazia |",
        "| `NEWARR`/`NEWMAP` | 60/61 | u16 n | containers a partir do topo da pilha |",
        "| `INDEX`/`SETIDX` | 62/63 | — | leitura/escrita indexada (bounds-check) |",
        "| `LEN`/`APPEND`/`HAS`/`KEYS` | 64–67 | — | operações de container |",
        "| `NATIVE` | 70 | u8 id, u8 argc | builtin nativo da VM (tabela abaixo) |",
        "",
        "> Saltos são **relativos** ao fim da própria instrução: `rel = alvo − (pc_após_operando)`.",
        "",
        "## Builtins nativos (NATIVE)",
        "",
        "A instrução `NATIVE` consome `argc` argumentos da pilha e empilha o resultado. A tabela de ids é espelhada entre o gerador (`NATIVES` em `codegen_pyro.py`) e a VM (`native()` em `vm/main.go`):",
        "",
        "| id | nome | id | nome | id | nome |",
        "|---|---|---|---|---|---|",
        "| 0 | `sqrt` | 7 | `round` | 14 | `trim` |",
        "| 1 | `pow` | 8 | `to_string` | 15 | `contains` |",
        "| 2 | `abs` | 9 | `to_int` | 16 | `find` |",
        "| 3 | `min` | 10 | `to_number` | 17 | `replace` |",
        "| 4 | `max` | 11 | `remove` | 18 | `substr` |",
        "| 5 | `floor` | 12 | `upper` | 19 | `split` |",
        "| 6 | `ceil` | 13 | `lower` | 20 | `join` |",
        "",
        "Enums não geram código: cada membro (`Nivel_ALTO`) vira uma constante inteira em tempo de compilação.",
        "",
        "## Desassemblando",
        "",
        "Veja os opcodes gerados para um programa:",
        "",
        "```bash",
        "python burnout/cryoc.py cryo/examples/example_containers.cryo --backend pyro --dis",
        "```"
      )
    },
    {
      slug: "pyro-vm", group: "Pyro — bytecode & VM", title: "Pyro: a VM",
      lead: "A VM Pyro (em Go) carrega, decodifica e executa o `.pyro`. A tipagem é dinâmica em runtime.",
      body: md(
        "## Tipagem dinâmica",
        "",
        "Os valores carregam o tipo em runtime (int64, float64, bool, string, null) e as operações são resolvidas pela VM:",
        "",
        "- `ADD` com algum operando string → **concatenação** (o outro é convertido).",
        "- Aritmética com algum `float` → promoção para float; senão, inteiro.",
        "- `EQ`/`NE` comparam por valor entre tipos compatíveis.",
        "",
        "## Segurança na VM",
        "",
        "- `DIV`/`MOD` inteiros por zero **abortam** (`[Cryo Seguranca] DivisaoPorZero`).",
        "- `ASSERT` aborta com a mensagem se a condição for falsa.",
        "",
        "## Compilação e execução",
        "",
        "A VM é compilada uma vez (Go) e reutilizada. O compilador cuida disso automaticamente com `--run`:",
        "",
        "```bash",
        "python burnout/cryoc.py cryo/examples/example_bytecode.cryo --backend pyro --run",
        "```",
        "",
        "Internamente: `go build` da VM em `pyro/vm/` gera `build/pyrovm`, que então carrega e executa o `.pyro`. Se o Go não estiver instalado, o `.pyro` é gerado mesmo assim (só não é executado).",
        "",
        "## Referências no código",
        "",
        "- **Gerador:** `burnout/codegen_pyro.py` (opcodes e serialização).",
        "- **VM:** `pyro/vm/main.go` (carga, decodificação e execução).",
        "- **Desassembler:** `burnout/disasm_pyro.py`."
      )
    },

    // =========================================================
    //  ECOSSISTEMA
    // =========================================================
    {
      slug: "editor", group: "Ecossistema", title: "Extensões VS Code",
      lead: "Três extensões cobrem o sistema no editor: **Cryo** (linguagem), **Pyro** (bytecode) e **Burnout** (o compilador).",
      body: md(
        "## As três extensões",
        "",
        "| Pasta | Extensão | O que faz |",
        "|---|---|---|",
        "| `cryo-vscode` | **Cryo Lang** | Realce de sintaxe, snippets e config da linguagem `.cryo` |",
        "| `pyro-vscode` | **Pyro Bytecode** | Realce do disassembly (`.pyasm`) + comando para desmontar `.pyro` |",
        "| `burnout-vscode` | **Burnout Compiler** | Comandos de compilar/rodar, auditar, tokens/AST + diagnósticos |",
        "",
        "## Cryo Lang",
        "",
        "Realça toda a sintaxe da linguagem — tipos, `schema`/`tool`/`skill`, `spawn`/`await`, builtins de LLM (`llm`, `agent`, `schema_of`), rede (`http_get`, `sleep`) e máquina (`pyro_open`, `pyro_write_file`). Inclui snippets para `fn`, `struct`, `schema`, `tool`, `agent`, `llm`, `spawn`, `map`, `try` e mais.",
        "",
        "## Pyro Bytecode",
        "",
        "O `.pyro` é binário, então o valor está no **disassembly**. A extensão realça o texto de disassembly (`.pyasm`) e adiciona o comando:",
        "",
        "- **Pyro: Desmontar .pyro** — clique com o botão direito num `.pyro` (ou use a paleta); roda o `disasm_pyro` e abre o listing legível numa nova aba.",
        "",
        "Configuração: `pyro.pythonPath`, `pyro.burnoutPath`.",
        "",
        "## Burnout Compiler",
        "",
        "Traz o compilador para dentro do editor:",
        "",
        "| Comando | Ação |",
        "|---|---|",
        "| Compilar e rodar | backend padrão + `--run` (`Ctrl+Alt+R`) |",
        "| Compilar (escolher backend) | menu go / pyro / c / asm |",
        "| Auditoria de segurança | `--audit` no painel Burnout |",
        "| Desmontar bytecode | `--dis` do `.pyro` gerado |",
        "| Mostrar AST / tokens | inspeção do front-end |",
        "",
        "Os erros do compilador (léxico/sintático/codegen) viram **sublinhados** na linha e coluna corretas, via o *problem matcher* `$burnout`. Há também um **task provider** `burnout` para o `tasks.json`.",
        "",
        "Configuração: `burnout.pythonPath`, `burnout.compilerEntry`, `burnout.defaultBackend`, `burnout.safeMode`.",
        "",
        "## Instalando as extensões",
        "",
        "Para desenvolver/testar, abra a pasta da extensão no VS Code e aperte **F5** (abre uma janela *Extension Development Host*). Para empacotar e instalar:",
        "",
        "```bash",
        "npm install -g @vscode/vsce",
        "cd cryo-vscode && vsce package        # gera um .vsix",
        "code --install-extension cryo-lang-v2-0.9.0.vsix",
        "```",
        "",
        "> A extensão **Burnout Compiler** localiza o compilador automaticamente: procura `burnout/cryoc.py` no workspace e, se não achar, usa `python -m burnout` (o [pacote instalado](#/biblioteca))."
      )
    },

    {
      slug: "roadmap", group: "Ecossistema", title: "Roadmap",
      lead: "O plano de implementações para o Cryo e o Pyro evoluírem até suportar **grandes projetos** — em fases, com objetivo claro por fase.",
      body: md(
        "O arquivo canônico é o [`ROADMAP.md`](https://github.com/Victor-477) na raiz do projeto. Resumo:",
        "",
        "## Fase 4 — Robustez da linguagem (CRYO)",
        "",
        "Eliminar o que impede programas médios/grandes:",
        "",
        "- **Módulos Cryo** (`import \"arquivo.cryo\"`) — o maior bloqueador atual: todo programa é um arquivo único",
        "- `try/catch` e opcionais (`T?`/`??`/`x!`) na VM Pyro",
        "- **Checagem semântica prévia** (variável não declarada, tipos, retorno faltando) antes do codegen, com linha/coluna em todos os erros",
        "- Interpolação de strings (`\"total: ${x}\"`) e iteração de caracteres",
        "",
        "## Fase 5 — Pyro competitivo (VM forte)",
        "",
        "Fazer do `.pyro` um alvo de produção:",
        "",
        "- **JSON nativo na VM** (structs já são maps) e `input()`/HTTP como natives",
        "- **Otimizador de bytecode**: constant folding, dead-code, fusão de instruções",
        "- Saltos longos (i32), dispatch mais rápido (meta: benchmark contra Python/Lua)",
        "- Info de depuração (pc → linha) para stack traces legíveis; formato `.pyro` v2 versionado",
        "",
        "## Fase 6 — Ferramentas para projetos grandes",
        "",
        "- **LSP** (diagnósticos ao digitar, go-to-definition) — transforma o VS Code",
        "- `cryoc fmt` (formatador), framework de testes (`test fn` + `cryoc test`), REPL",
        "- Gerenciador de pacotes (`cryo.toml`), debugger da VM, CI dos exemplos",
        "",
        "## Fase 7 — Escala e novos alvos",
        "",
        "- Backend **WASM**; `spawn`/`await` na VM Pyro",
        "- **Generics**, interfaces/traits e pattern matching no Cryo",
        "- LLM com streaming; modo `--sandbox` da VM (natives de máquina/rede desligados por política)",
        "",
        "## Princípios",
        "",
        "1. **Módulos primeiro** — nenhum projeto grande cabe num arquivo.",
        "2. **Paridade do Pyro com o Go** — cada fase reduz a lista de \"use --backend go\".",
        "3. **Erro bom é feature** — posição, mensagem acionável e sugestão de backend.",
        "4. **Um recurso só entra com teste** — a suíte cresce junto.",
        "5. **A doc acompanha o código** — cada feature atualiza este site."
      )
    },

    // =========================================================
    //  REFERÊNCIA
    // =========================================================
    {
      slug: "builtins", group: "Referência", title: "Builtins",
      lead: "Funções embutidas da linguagem, agrupadas por área. A disponibilidade depende do backend.",
      body: md(
        "## Núcleo (todos os backends aplicáveis)",
        "",
        "| Builtin | Descrição |",
        "|---|---|",
        "| `print(v)` | imprime um valor |",
        "| `input(prompt)` | lê uma linha (`string`) |",
        "| `len(x)` | tamanho de string/array/map |",
        "| `assert(cond)` / `assert(cond, msg)` | aborta se falso |",
        "| `to_string(v)` `to_int(v)` `to_number(v)` | conversões |",
        "",
        "## Matemática (go / node / pyro / c)",
        "",
        "| Builtin | Descrição |",
        "|---|---|",
        "| `sqrt` `pow` `abs` | raiz, potência, valor absoluto |",
        "| `min` `max` | mínimo, máximo |",
        "| `floor` `ceil` `round` | arredondamentos |",
        "",
        "## Strings (go / node / pyro)",
        "",
        "| Builtin | Descrição |",
        "|---|---|",
        "| `upper(s)` / `lower(s)` | maiúsculas / minúsculas |",
        "| `trim(s)` | remove espaços das pontas |",
        "| `contains(s, sub)` | `true` se contém o trecho |",
        "| `find(s, sub)` | índice do trecho (ou `-1`) |",
        "| `replace(s, velho, novo)` | substitui todas as ocorrências |",
        "| `substr(s, inicio, n)` | recorte com limites seguros |",
        "| `split(s, sep)` | divide em `string[]` |",
        "| `join(arr, sep)` | junta um array em string |",
        "",
        "> Na VM Pyro, todos os builtins de matemática, conversão e string executam pela instrução [`NATIVE` da ISA](#/pyro-isa).",
        "",
        "## Containers (backend go / node / pyro)",
        "",
        "| Builtin | Descrição |",
        "|---|---|",
        "| `has(m, k)` | chave existe no mapa |",
        "| `keys(m)` | chaves do mapa (array) |",
        "| `remove(m, k)` | remove chave do mapa |",
        "| `arr.push(v)` | adiciona ao array |",
        "",
        "## SaaS / rede (backend go)",
        "",
        "| Builtin | Descrição |",
        "|---|---|",
        "| `json_encode(v)` | serializa para JSON |",
        "| `json_decode(s) as T` | desserializa tipado |",
        "| `http_get(url)` `http_post(url, body)` | cliente HTTP |",
        "| `sleep(ms)` | pausa |",
        "",
        "## LLM & agentes (backend go)",
        "",
        "| Builtin | Descrição |",
        "|---|---|",
        "| `schema_of(T)` | JSON Schema de um tipo |",
        "| `llm(m, p)` / `llm(m, p) as T` | completion / structured output |",
        "| `tools()` `tools_json()` `tool_get(n)` | introspecção de tools |",
        "| `agent(m, p)` / `agent(m, p, [tools], passos)` | laço de tool-calling |",
        "| `skills()` `skill_get(n)` `skill_has(n)` `skills_json()` | introspecção de skills |",
        "",
        "## Máquina (backend go)",
        "",
        "| Builtin | Descrição |",
        "|---|---|",
        "| `pyro_exec(cmd)` | executa comando |",
        "| `pyro_env(n)` | variável de ambiente |",
        "| `pyro_args()` | argumentos da CLI |",
        "| `pyro_time()` | timestamp |",
        "| `pyro_read()` | lê uma linha |",
        "| `pyro_write_file(path, s)` | escreve arquivo (`bool`) |",
        "| `pyro_open(path)` | abre no app padrão do SO (`bool`) |",
        "| `pyro_exit(cod)` | encerra o processo |"
      )
    },
    {
      slug: "palavras-chave", group: "Referência", title: "Palavras-chave",
      lead: "Todas as palavras reservadas do Cryo, reconhecidas pelo lexer.",
      body: md(
        "## Declaração & tipos",
        "",
        "`fn` · `return` · `const` · `struct` · `enum` · `new` · `schema` · `tool` · `skill` · `library` · `import`",
        "",
        "Tipos: `int` · `number` · `string` · `bool` · `void` · `map` · `future`",
        "",
        "## Controle de fluxo",
        "",
        "`if` · `else` · `for` · `while` · `do` · `in` · `switch` · `case` · `default` · `break` · `continue`",
        "",
        "## Erros & segurança",
        "",
        "`try` · `catch` · `finally` · `assert` · `safe` · `unsafe`",
        "",
        "## Concorrência",
        "",
        "`spawn` · `await` · `future`",
        "",
        "## Valores & conversão",
        "",
        "`true` · `false` · `null` · `as`",
        "",
        "## Tokens especiais",
        "",
        "| Token | Significado |",
        "|---|---|",
        "| `={` | abre o corpo de `fn`/`tool`/`skill` |",
        "| `->` | tipo de retorno de função |",
        "| `??` | null-coalescing |",
        "| `?` | sufixo de tipo opcional (`T?`) e ternário |",
        "| `!` | unwrap de opcional (`x!`) e negação lógica |",
        "| `import >Lang<` | habilita a linguagem estrangeira `Lang` (exigido p/ blocos) |",
        "| `>Lang( ... )` | bloco de código estrangeiro (requer `import >Lang<`) |",
        "| `library >lang nome<` | importa uma biblioteca da linguagem `lang` |"
      )
    },
    {
      slug: "exemplos", group: "Referência", title: "Exemplos",
      lead: "Programas prontos em `cryo/examples/`, cobrindo do núcleo aos agentes de IA.",
      body: md(
        "| Exemplo | Demonstra | Backend |",
        "|---|---|---|",
        "| `example_bytecode.cryo` | núcleo → `.pyro` + VM | pyro |",
        "| `example_containers.cryo` | arrays / maps / structs | pyro |",
        "| `example_stdlib.cryo` | stdlib (math/strings/enum/conversões) nos 3 backends | pyro/go/node |",
        "| `example_go.cryo` | linguagem completa | go |",
        "| `example_saas.cryo` | mapas + JSON + opcionais | go |",
        "| `example_async.cryo` | concorrência (spawn/await) + HTTP | go |",
        "| `example_llm.cryo` | schema + `llm ... as T` + tool | go |",
        "| `example_agent.cryo` | laço de agent (tool-calling) | go |",
        "| `example_agent_pedido.cryo` | agente de e-commerce com 6 tools | go |",
        "| `example_agent_landpage.cryo` | agente que cria e abre uma landing page | go |",
        "| `example_pyro.cryo` | skills nativas + acesso à máquina | go |",
        "| `example_foreign.cryo` | blocos estrangeiros verificados + libraries | go/c |",
        "| `example_go_http.cryo` | bloco Go usando net/http via `library` | go |",
        "| `example_node.cryo` | núcleo do Cryo em JavaScript + `library >node os<` | node |",
        "| `example_node_fs.cryo` | grava/lê arquivo via `library >node fs<` | node |",
        "| `example_v4.cryo` | switch, bitwise, literais, assert, `>C(...)` | c |",
        "| `example_asm.cryo` | subconjunto inteiro | asm |",
        "| `example_struct.cryo` | retorno de struct em registrador | asm |",
        "",
        "## Rodando um exemplo",
        "",
        "```bash",
        "python burnout/cryoc.py cryo/examples/example_agent_pedido.cryo --backend go --run",
        "```",
        "",
        "## Testes",
        "",
        "A suíte de fumaça valida todos os geradores de código:",
        "",
        "```bash",
        "python burnout/tests/test_smoke.py",
        "```"
      )
    }
  ];

  // Grupos da sidebar (ordem = ordem de navegação / pager)
  var groups = [
    { title: "Primeiros passos", pages: ["introducao", "arquitetura", "instalacao", "inicio-rapido"] },
    { title: "A linguagem Cryo", pages: ["sintaxe", "tipos", "funcoes", "controle-de-fluxo", "operadores", "structs-enums", "arrays", "mapas", "opcionais", "json", "erros"] },
    { title: "Concorrência & rede", pages: ["concorrencia", "http"] },
    { title: "LLM & Agentes", pages: ["schema", "tools", "agent", "llm-real", "skills", "maquina"] },
    { title: "Compilador Burnout", pages: ["cli", "backends", "estrangeiros", "seguranca", "biblioteca"] },
    { title: "Pyro — bytecode & VM", pages: ["pyro-formato", "pyro-isa", "pyro-vm"] },
    { title: "Ecossistema", pages: ["editor", "roadmap"] },
    { title: "Referência", pages: ["builtins", "palavras-chave", "exemplos"] }
  ];

  window.DOCS = { pages: pages, groups: groups };
})();
